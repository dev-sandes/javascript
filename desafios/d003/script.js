function verificar() {
    let num = window.prompt('Digite um número inteiro qualquer')
    int = Number(num)
    window.alert(`Antes do ${int}, temos o número ${int-1}.
Depois do ${int}, temos o número ${int+1}.`)
}
